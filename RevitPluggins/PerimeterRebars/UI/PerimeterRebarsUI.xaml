<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="RevitPluggins.PerimeterRebars.PerimeterRebarsUI"
        Title="PERIMETER REBARS"
        SizeToContent="Height"
        Width="486"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="ComboButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FDFDFD"/>
            <Setter Property="BorderBrush" Value="#ADB2B5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E5E5"/>
                    <Setter Property="BorderBrush" Value="#707070"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#F0F0F0"/>
                    <Setter Property="Foreground" Value="#A0A0A0"/>
                    <Setter Property="BorderBrush" Value="#D3D3D3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#444"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <StackPanel>
            <GroupBox Header="FLOOR SELECTION">
                <Grid Height="30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="SELECTION METHOD" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <RadioButton x:Name="RbContinuousFloor" Content="CONTINUOUS FLOOR" Margin="0,0,10,0" GroupName="SelectionGroup" IsChecked="True"/>
                        <RadioButton x:Name="RbFilledRegion" Content="FILLED REGION" GroupName="SelectionGroup"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GroupBox Header="EDGE COLUMNS">
                <Grid Height="30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="MAX DISTANCE FROM EDGE (ft)" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtMaxEdgeDistance" Grid.Column="1" VerticalAlignment="Center"
                             Text="20" Padding="4,2" BorderBrush="#ADB2B5" BorderThickness="1"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="COLUMN GRID">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Text="GRID SEARCH RADIUS (ft)" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtGridSearchRadius" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center"
                             Text="60" Padding="4,2" BorderBrush="#ADB2B5" BorderThickness="1"/>
                    <TextBlock Grid.Row="2" Text="CLUSTER TOLERANCE (ft)" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtGridClusterTolerance" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center"
                             Text="15" Padding="4,2" BorderBrush="#ADB2B5" BorderThickness="1"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="SLAB">
                <Grid Height="30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="SLAB THICKNESS (in)" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBox x:Name="TxtSlabThickness" Grid.Column="1" VerticalAlignment="Center"
                             Text="10" Padding="4,2" BorderBrush="#ADB2B5" BorderThickness="1"/>
                </Grid>
            </GroupBox>

            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="BtnDefine"
                        Grid.Column="0"
                        Content="DEFINE REGIONS"
                        Height="35"
                        FontWeight="Bold"
                        Style="{StaticResource ComboButtonStyle}"
                        Click="BtnDefine_Click"/>

                <Button x:Name="BtnAddRebars"
                        Grid.Column="2"
                        Content="ADD REBARS"
                        Height="35"
                        FontWeight="Bold"
                        IsEnabled="False"
                        Style="{StaticResource ComboButtonStyle}"
                        Click="BtnAddRebars_Click"/>
            </Grid>
        </StackPanel>
    </Grid>
</Window>